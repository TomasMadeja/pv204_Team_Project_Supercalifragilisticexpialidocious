package cz.muni.fi.pv204.javacard.crypto;

public class MagicAes {

    public void generateKey(byte[] data) {}

    public short encrypt(
            byte[] inBuffer,
            short inOffset,
            short inLen,
            byte[] outBuffer,
            short outOffset,
            short outLen
    ) {
        return (short) 0;
    }

    public short decrypt(
            byte[] inBuffer,
            short inOffset,
            short inLen,
            byte[] outBuffer,
            short outOffset,
            short outLen
    ) {
        return (short) 0;
    }
}
